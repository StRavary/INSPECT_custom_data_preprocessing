type: model_2d
model_name: resnext_101_ct
pretrain_type: supervised
num_class: 1
checkpoint_path: "/data/processed/INSPECT/checkpoints/resnext101_ct.ckpt"
# Pure CT-pretrained ResNeXt101-32x8d (BigTransfer, no RSNA fine-tuning).
# Use this as the starting point for RSNA fine-tuning with channels=window.
# After fine-tuning, copy the best checkpoint to resnext_101_ct.yaml.
