type: model_2d
model_name: resnetv2_101_ct
pretrain_type: supervised
num_class: 1
# RSPECT (RSNA PE) fine-tune of resnetv2_ct_pretrain.yaml's official
# StanfordShahLab/resnetv2_ct checkpoint, with the corrected channels=window
# (3 distinct windows, not repeated PE window) — run 2026-08-10_21:25:26.
# Best val/mean_auroc (0.931) was at epoch 0 and never improved afterward
# (save_top_k=1 kept only that checkpoint), so this is only ~1 epoch of
# fine-tuning past the base HF checkpoint.
checkpoint_path: "/data/processed/INSPECT/checkpoints/classify_pe_present_on_image_2026-08-10_21:25:26/epoch=0-val/mean_auroc=0.931.ckpt"
