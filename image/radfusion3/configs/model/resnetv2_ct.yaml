type: model_2d
model_name: resnetv2_101_ct
pretrain_type: supervised
num_class: 1
# Was pointing at a 2026-07-24 local RSNA fine-tune that predates the
# channels=window fix (repeated single window -> 3 distinct windows) and was
# almost certainly trained on the buggy input distribution. Replaced with the
# official StanfordShahLab/resnetv2_ct HuggingFace checkpoint instead — same
# checkpoint run_rsna.sh now starts fine-tuning from (see
# resnetv2_ct_pretrain.yaml), so this file is ready to use as-is for
# run_featurize.py even without re-running run_rsna.sh first. If you do
# re-run run_rsna.sh, copy its best checkpoint here afterward, same as before.
checkpoint_path: "/data/processed/INSPECT/checkpoints/resnetv2_ct.ckpt"
